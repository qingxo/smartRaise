import { CardCheckDirective } from './card-check.directive';

describe('cardCheckDirective', () => {
  it('should create an instance', () => {
    const directive = new CardCheckDirective();
    expect(directive).toBeTruthy();
  });
});
