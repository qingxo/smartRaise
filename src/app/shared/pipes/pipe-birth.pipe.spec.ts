import { PipeBirthPipe } from './pipe-birth.pipe';

describe('PipeBirthPipe', () => {
  it('create an instance', () => {
    const pipe = new PipeBirthPipe();
    expect(pipe).toBeTruthy();
  });
});
